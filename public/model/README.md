# Model artifact

Place the exported browser model here:

```text
public/model/tiny_denoiser.onnx
```

The file is intentionally not committed by default because trained checkpoints
can become large. The GitHub Pages build will include it when present.

