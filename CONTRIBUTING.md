# Contributing

Contributions are welcome. The project is intentionally small, so changes are
easiest to review when they keep one concern per pull request:

- simulator or dataset changes
- diffusion model changes
- browser runtime changes
- documentation updates

Before opening a pull request, run:

```bash
npm run build
python -m compileall world_model_lab
```

